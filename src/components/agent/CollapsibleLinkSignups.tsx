// CollapsibleLinkSignups - DB calls stubbed for performance
interface CollapsibleLinkSignupsProps {
  isOpen?: boolean;
  onToggle?: () => void;
}

export function CollapsibleLinkSignups({ isOpen, onToggle }: CollapsibleLinkSignupsProps) {
  // Referral DB queries removed to reduce database load
  return null;
}
